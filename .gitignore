# Outreach drafts (Reddit posts, Twitter copy, etc.)
# Kept local-only on purpose — drafts shouldn't appear in the public repo.
marketing/

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo

# Behavioral test run outputs (per-run LLM output; regenerated each run, never committed)
tests/behavioral/*/result.md
__pycache__/

# Deferral-gate per-session tally (ephemeral churn, not a record; regenerated each session)
.unforget-session.json

# Audit reports (bug-echo / radar-suite / etc.) — per-run analysis artifacts, not source
.agents/
