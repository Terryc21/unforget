# unforget v0.2 Release Plan

**Status:** In progress. Phase 1 (plugin packaging) ✅ shipped 2026-05-02. Phase 2 (Tier 2 recoverability) ✅ shipped 2026-05-02. Phases 3-5 pending.

**Companion docs:**
- [`v0.2-roadmap.md`](v0.2-roadmap.md) — *what* ships in v0.2 (spec proposals, the content)
- This file — *how and when* v0.2 ships (execution sequence, dependencies, ship criteria)
- [`test-reports/2026-05-02-stuffolio-fresh-init-simulation.md`](test-reports/2026-05-02-stuffolio-fresh-init-simulation.md) — empirical basis for Tier 6 fixes

---

## Scope decision (from 2026-05-02 planning conversation)

**v0.2 ships** plugin packaging + safety + must-ship Tier 6 fixes only. **v0.3 holds** the new subcommands (`/unforget close`, `/unforget delete`) and the permanent design decisions (External Target value, Hidden Status value, archive policy).

Why this split:
- Plugin packaging is the most user-visible change. Shipping it alone, with critical safety + adoption-path fixes, gets adopters using the plugin form. Anything beyond that is bonus.
- Tier 4 features are non-trivial design work. Holding them lets v0.2 ship in weeks, not months.
- Tier 3 design decisions need beta feedback before locking in. Better to ship v0.2 to a small audience, get feedback, then commit Tier 3 in v0.3.
- v0.2 can validate the plugin packaging path itself, separate from validating new features.

**v0.2 ship criteria:**
1. Plugin packaging works. Adopters install via Claude Code plugin manager (or git-clone-then-install pattern, whichever the platform supports). Invocation drops the `/skill` prefix; users type `/unforget init` directly.
2. All Tier 1 safety items remain in place (header notice, format-version marker, recovery section, example file).
3. Tier 2 recoverability lands (R1 dry-run, R2 backup, R3 format-version detection on read).
4. Must-ship Tier 6 surface-survey fixes land (T-2 filename archive exclusion, T-5 radar-suite format compatibility, T-6 .agents/plans path, T-10 general doc scanning, T-11 roadmap-file detection).
5. The Stuffolio fresh-init re-test produces ≥70% of expected candidates (target: 42 of 60 rows). v0.1 baseline was ~9 of 60.
6. README updated to reflect plugin install + invocation form. Version bumped to 0.2.0.
7. SKILL.md / plugin manifest version-stamped.

---

## Phase 1 — Foundation: plugin packaging ✅ (shipped 2026-05-02)

**Status:** Complete. Commits `b790868` (scaffold) and `ae9892e` (docs). Live install verified: `/plugin marketplace add Terryc21/unforget` + `/plugin install unforget@unforget` succeeds, `/unforget` responds in a fresh session without the `/skill` prefix, backward-compat against Stuffolio's v0.1-format UNFORGET.md confirmed (skill correctly detected the existing file). Layout decision: flat single-skill (SKILL.md at repo root, manifest references it directly).

**Goal:** Move from SKILL.md-only to Claude Code plugin (`.claude-plugin/`), with no behavior changes from v0.1.x.

**Why first:** all subsequent phases edit the spec content. If plugin packaging is left until last, we have to retest every Tier 6 change twice (once in SKILL.md form, once in plugin form). Doing packaging first means every later change is tested in its final form.

### Tasks

1. **Reference an existing Claude Code plugin** to copy structure from. radar-suite has been mentioned as plugin-based in user discussions; check its `.claude-plugin/` layout.
2. **Create plugin scaffold:**
   - `.claude-plugin/manifest.json` (or .toml depending on convention) with name, version, description, slash commands.
   - Slash command bindings for the 7 v0.1 subcommands (`init`, `add`, `edit`, `import`, `list`, `scan`, `promote`).
   - Plugin entry point that loads SKILL.md content (or replaces it with plugin-resident files).
3. **Verify invocation:** install the plugin locally, type `/unforget` in a fresh Claude Code session, confirm it responds. The `/skill` prefix should no longer be required.
4. **Verify backward compat:** running v0.2 against Stuffolio's existing v1-format UNFORGET.md should produce no errors. The format-version marker (R3) handles this in Phase 2; for now, just confirm no crashes.
5. **Update install docs:** README "Install" section becomes plugin-install instructions. Manual install path (the `cp SKILL.md` fallback) preserved as secondary option.
6. **Commit:** "v0.2 Phase 1: plugin packaging, no behavior changes"

### Open questions for Phase 1 — resolved 2026-05-02

- **Q: Does the Claude Code plugin format support multiple slash commands per plugin, or is each subcommand a separate plugin?**
  A: One plugin can declare multiple skills (radar-suite has 8). Subcommands within a single skill (e.g., `/unforget init`, `/unforget add`) are handled by the skill itself, not split into separate plugin entries. unforget ships as one plugin with one skill that handles all 7 subcommands internally.
- **Q: Does the plugin load SKILL.md as content, or replace it with plugin-native command files?**
  A: Plugin loads SKILL.md as content (verified by working radar-suite layout and confirmed by live test — `/unforget` invocation triggered the existing SKILL.md spec). No native command files needed; SKILL.md remains the spec.
- **Q: How does v0.2 plugin coexist with v0.1 SKILL.md install if both are present in `~/.claude/skills/unforget/`?**
  A: Not tested directly. The v0.1 fallback documentation now says "either install path works, but don't run both at once." If a user has both, Claude Code's plugin manager should win (loaded earlier in the discovery order), but this is environment-dependent. Document as a caveat rather than fix programmatically.

### Phase 1 ship gate — passed 2026-05-02

- ✅ Plugin loads via marketplace install (`/plugin marketplace add Terryc21/unforget` + `/plugin install unforget@unforget`).
- ✅ `/unforget` responds in a fresh session without `/skill` prefix.
- ✅ Backward-compat: skill correctly detected and read Stuffolio's existing v0.1-format UNFORGET.md.
- ⏸️ `/unforget --version` (D4) deferred to Phase 5 — that's a doc/install-verification command, not a ship gate for plugin packaging itself.
- ⏸️ Final version bump to `0.2.0` deferred to Phase 5 (currently `0.2.0-phase1` pre-release tag during phase work).

### Next session: Phase 2 (Tier 2 recoverability)

Phase 2 ships R1 (`--dry-run` flag on promote), R2 (auto-backup before destructive operations), and R3 (format-version detection on every read). These are spec edits to `SKILL.md`, gated behind the now-validated plugin install. Continuation prompt below — paste verbatim into a fresh session opened on `/Volumes/2 TB Drive/Coding/GitHub/unforget/`.

---

#### Continuation prompt — unforget v0.2 Phase 2 (Tier 2 recoverability)

##### Context

Pick up unforget v0.2 Phase 2 from `/Volumes/2 TB Drive/Coding/GitHub/unforget/v0.2-release-plan.md`. Phase 1 (plugin packaging) shipped 2026-05-02 in commits `b790868`, `ae9892e`, `2abd0ea`. Live install verified: `/plugin marketplace add Terryc21/unforget` + `/plugin install unforget@unforget` works, `/unforget` responds without `/skill` prefix, backward-compat against v0.1-format files confirmed. Phase 2 is the first phase that edits the spec content.

##### Read first

- `v0.2-release-plan.md` Phase 2 section — the three tasks (R1, R2, R3) plus ship gate
- `v0.2-roadmap.md` Tier 2 section — the original proposals these tasks come from
- `SKILL.md` — current spec at v0.1.0 (frontmatter `version` field intentionally not bumped yet; bumps to `0.2.0` only at Phase 5)
- `examples/UNFORGET.md` — the canonical example file; check the format-protection header and format-version marker (already in place from earlier safety work)

##### What to decide first

Two design calls before writing spec text:

1. **R1 dry-run UX.** Two patterns to choose from:
   - **Inline diff in chat:** `promote --dry-run` renders the would-apply changes as a markdown diff in the Claude Code conversation. User reads the diff, replies "apply" or "cancel."
   - **Two-step write:** dry-run writes to `UNFORGET.md.dry-run-<timestamp>` (a sibling file user can open and read), prints a path, prompts to apply. Survives session loss.

   Pick one. Inline is simpler; two-step is recoverable across sessions. Recommend inline unless you've heard of a specific case where the user lost the diff before deciding.

2. **R2 backup retention policy.** Plan says "keep last 5 backups; delete older ones automatically." Decide whether deletion is silent or announced. Silent = clean. Announced = user knows what happened. Recommend silent with a one-line note in the SKILL.md spec ("backup files older than the 5 most recent are pruned during each `promote`"). No prompt.

##### Surface area of the work

Whichever decisions get made:

1. **`SKILL.md` `## /unforget promote` section** (currently lines 557+). Add `--dry-run` flag documentation. Spec: what the flag accepts, what it produces, how the user confirms apply.
2. **`SKILL.md` new section: `## Backups and recovery`** (or fold into existing "Recovering a broken UNFORGET.md" in README). Document the `.bak-YYYY-MM-DD-HHMMSS` naming, retention policy, `.gitignore` recommendation. Reuse the recovery section that already exists in README — don't duplicate prose.
3. **`SKILL.md` `## Compatibility notes` section** (line 611+). Add R3 format-version marker handling: absent → prompt, v1 → proceed, v2+ → refuse writes (read-only). Explicit error messages for each case.
4. **`examples/UNFORGET.md` format-version marker.** Verify it has `<!-- unforget-format: v1 -->` near the top. If missing, add it (this is the canonical reference file and v0.2's R3 logic depends on it).
5. **Frontmatter update in `SKILL.md`.** The frontmatter `Subcommands` block lists 7 subcommands. R1 doesn't add a new subcommand; it adds a flag. No change needed to the list. **Do not bump `version: 0.1.0`** — that happens at Phase 5.
6. **README install section.** No changes needed for Phase 2. Backups + dry-run + version detection are spec-level features the install path doesn't touch.

##### Constraints to honor

- **No spec drift.** Phase 2 changes spec content, but the changes must be additive — don't rewrite existing sections. Add to `## /unforget promote`, add new `## Backups and recovery`, extend `## Compatibility notes`.
- **No version bump.** Frontmatter `version` stays at `0.1.0` until Phase 5. Manifest at `0.2.0-phase2` (bump from `0.2.0-phase1`).
- **No emojis in committed prose** unless the prose is showing what the user will see in their UNFORGET.md (where emojis like 🚢 / 🌫️ are part of the Target column format).
- **No em dashes, no double hyphens.** Restructure sentences instead.
- **No co-authored-by lines in commits.** Push via GitHub Desktop, not Terminal.
- **Verify the existing examples still parse.** After spec edits, re-run `bash .claude-plugin/verify-manifest.sh`. The example `UNFORGET.md` should still be valid against the new spec.
- **Two-question interview opt-in.** This is spec design; warrants the interview being on. Ask at start of session.

##### Verification

After Phase 2 ships:

1. `bash .claude-plugin/verify-manifest.sh` passes (no manifest drift).
2. The two design decisions (R1 UX, R2 retention) are recorded in the release plan's Phase 2 section as resolved questions.
3. Manual repro in a fresh Claude Code session: install Phase 2 build, run `/unforget promote --dry-run` against a sandbox copy, confirm the dry-run UX matches the chosen pattern. Backup files appear with the expected naming. R3 marker handling: synthesize a `<!-- unforget-format: v0.5 -->` test file, confirm refuse-to-write behavior.
4. Stamp `v0.2-release-plan.md` Phase 2 section with `✅ shipped <date>` and commit hashes, mirroring the Phase 1 stamp.
5. Bump manifest version: `0.2.0-phase1` → `0.2.0-phase2` in both `plugin.json` and `marketplace.json`.

##### Out of scope for this session

- Phase 3 (Tier 6 must-ship — T-2, T-5, T-6, T-10, T-11). Surface-survey work.
- Phase 4 (Tier 6 polish — T-3, T-4, T-7, T-8, T-9).
- Phase 5 (final ship — version bump to `0.2.0`, push, tag, announce).
- Stuffolio re-test against the scratch copy. Phase 2 has zero surface-survey changes; release plan exempts it from re-test (per "Test gates between phases" table).

##### What to do first when this prompt is loaded

1. Read the four "Read first" files above.
2. Apply the two-question interview opt-in (CLAUDE.md Decision Workflow). Default On for this session — Phase 2 is design-heavy.
3. Ask the user the R1 (dry-run UX) and R2 (retention announcement) questions. Don't guess.
4. After answers, write a short plan file at `~/.claude/plans/<auto-named>.md` with the spec sections to edit, the design decisions captured, and the verification steps from above. Get plan approval via `ExitPlanMode`.
5. Then execute. Three commits (R1 spec, R2 spec, R3 spec), each with `bash .claude-plugin/verify-manifest.sh` passing before commit. Final commit stamps the release plan.

---

## Phase 2 — Tier 2 recoverability ✅ (shipped 2026-05-02)

**Status:** Complete. Commits `7f82189` (R1 dry-run spec), `e4a4526` (R2 auto-backup spec), `d0f866b` (R3 format-version detection + manifest bump from `0.2.0-phase1` to `0.2.0-phase2` + initial plan stamp). All three edits to `SKILL.md` are additive; no existing sections were rewritten. SKILL.md frontmatter `version` stays at `0.1.0` until Phase 5.

**Goal:** Make destructive operations safe to refine. R1, R2, R3 are prerequisites for Phase 3 confidence.

### Resolved questions (Phase 2 design decisions)

| ID | Decision | Rationale |
|---|---|---|
| R1 dry-run UX | Inline diff in chat | Simpler surface area; no orphaned sibling files in the user's working directory. `promote` is an interactive ritual, not a long-running unattended job, so cross-session recoverability is acceptable to forgo. |
| R2 retention | Silent prune with spec note | Pruning happens silently during each `promote`. The retention policy (keep 5 most recent) is documented in `SKILL.md`'s new "Backups and recovery" section so the behavior is discoverable without being noisy on every run. |
| R3 marker handling | Three explicit cases | Marker absent prompts (default no). `v1` proceeds. Future version (`v2+`) operates in read-only mode and refuses writes with a clear error pointing to the version mismatch. |

### Tasks

1. **R1 — `--dry-run` flag on `promote`:**
   - Add to spec: `/unforget promote --dry-run` shows the diff without applying.
   - Implementation: capture the planned changes, render them as a markdown diff, prompt user "apply?" before write.
   - Test: run on Stuffolio's UNFORGET.md scratch copy with --dry-run; confirm no file changes.
2. **R2 — Auto-backup before destructive operations:**
   - Spec: before `promote` writes, create `UNFORGET.md.bak-YYYY-MM-DD-HHMMSS` in the same directory.
   - Keep last 5 backups; delete older ones automatically.
   - Add `*.bak-*` to a `.gitignore` template the init flow offers to apply.
   - Test: run promote twice in a day; confirm two backups present.
3. **R3 — Format-version detection on every read:**
   - Spec: `add` / `list` / `promote` / `scan` / `edit` open the file, check for `<!-- unforget-format: vN -->` marker.
   - If marker absent: prompt user "this file may not be in unforget format; proceed anyway?" Default no.
   - If marker recognized (v1): proceed normally.
   - If marker is a future version (v2+ when the skill is v0.2 reading v1): proceed read-only with warning, refuse writes.
   - Test: synthesize a v0.5 marker file, confirm v0.2 refuses to write to it.

### Phase 2 ship gate

`/unforget promote --dry-run` produces a clean diff. Backups appear after `promote` runs. A v0.1 file (no format-version marker) prompts the user and proceeds with consent. A future-version marker file is refused.

---

## Phase 3 — Critical Tier 6 fixes ✅ (shipped 2026-05-02)

**Status:** Complete. Commits `6b64d95` (T-2 archive filename exclusion), `6ed6aef` (T-5 radar-suite v3+ format split), `14830b0` (T-6 .agents/plans/ scan path), `32b5fdc` (T-10 Surface 1b general doc scanning), `c4f9ad6` (T-11 roadmap-shaped file detection). Manifest bumped from `0.2.0-phase2` to `0.2.0-phase3`. All five edits to `SKILL.md` are additive (T-2/T-6/T-11 extend existing sentences; T-5 rewrites Surface 2 only; T-10 adds a new section between existing ones). SKILL.md frontmatter `version` stays at `0.1.0` until Phase 5.

**Goal:** Fix the surface-survey gaps that prevent fresh init from finding real deferred work. After this phase, the Stuffolio re-test should jump from ~9 candidates to ~30+.

### Resolved questions (Phase 3 design decisions)

| ID | Decision | Rationale |
|---|---|---|
| T-5 radar-suite source | Both: scratch reports as per-finding source + `ledger.yaml` as audit-run metadata; pin radar-suite v3+ schema | Maximum coverage. Per-finding text comes from `scratch/*-radar-*-YYYY-MM-DD.md` (prose-friendly markdown that survives radar-suite schema bumps). `ledger.yaml` is read for `audit_started`, `sessions[].skill`, and `sessions[].timestamp` only (recency + skill-attribution signals that prose alone cannot supply). Other `.radar-suite/*.yaml` files are explicitly skipped. |
| T-10 heuristic strictness | All six heuristics, each prompts the user (no auto-import) | Prose-shape detection is fuzzy. A heading containing "Deferred" can mean three different things in three different docs; user confirmation is the safety net. The six: heading text matches `Deferred` / `Pending` / `TODO` / `Phase N pending`; date-prefix filenames; explicit `DEFERRED:` heading prefixes. |

### Tasks

1. **T-2 — Filename archive exclusion:**
   - Spec change in Phase 2 universal exclusion rule: also skip files matching `*archive*.md` (case insensitive) regardless of path.
   - Test: run scan against scratch Stuffolio; confirm `Deferred-archive-2026-05-01.md` is skipped.
2. **T-5 — radar-suite format compatibility:**
   - Decide: pin radar-suite version + update spec to v3 schema, OR use scratch reports (`scratch/*-radar-*.md`) as the deferral source.
   - Recommend: do both. Pin radar-suite v3+ as the supported format. Use scratch reports as the per-finding source. Use ledger.yaml only for "was this audit run?" metadata.
   - Update SKILL.md Phase 2 Surface 2 description.
   - Test: run scan against scratch Stuffolio; confirm radar-suite findings are extracted.
3. **T-6 — Add `.agents/plans/` to default scan paths:**
   - Spec change: Phase 2 Surface 3 default scan paths become `./plans/`, `./.claude/plans/`, `./.agents/plans/`. Generalize to `**/plans/*.md` excluding archive paths.
   - Test: run scan against scratch Stuffolio; confirm 4 plan files in `.agents/plans/` are found.
4. **T-10 — General doc scanning:**
   - Spec addition: new Surface 1b. Scan `Documentation/Development/*.md`, `Documentation/Notes/*.md`, `scratch/*.md`, and equivalents for files whose content shape suggests deferral artifacts.
   - Heuristics: heading text matching "Deferred", "Pending", "TODO", "Phase N pending"; date-prefix file names; explicit "DEFERRED:" prefixes in headings.
   - Each match prompts user for import decision (don't auto-import; prose-shape detection is fuzzy).
   - Test: run scan against scratch Stuffolio; confirm `2026-04-30-test-suite-failures.md` in `Documentation/Development/` is offered as a candidate.
5. **T-11 — Recognize roadmap-shaped files:**
   - Spec change: Surface 1 regex extended to also match `*roadmap*.md`, `ROADMAP.md`, `*plan*.md` (case insensitive).
   - Plus content-shape heuristic: 3+ headings matching "Tier N" / "Phase N" / "Priority N" patterns flag a file as a deferral artifact.
   - Test: run scan against the unforget public repo itself; confirm `v0.2-roadmap.md` and this file (`v0.2-release-plan.md`) are both surfaced.

### Phase 3 ship gate

Stuffolio re-test produces ≥30 candidates (vs ~9 baseline). The unforget self-test (init run on the unforget repo) finds at least the v0.2 roadmap and release plan.

---

#### Continuation prompt: unforget v0.2 Phase 3 (Tier 6 must-ship surface-survey fixes)

##### Context

Pick up unforget v0.2 Phase 3 from `/Volumes/2 TB Drive/Coding/GitHub/unforget/v0.2-release-plan.md`. Phases 1 (plugin packaging) and 2 (Tier 2 recoverability) shipped on 2026-05-02 (Phase 2 commits `7f82189`, `e4a4526`, `d0f866b`, `25a4df8`). Plugin install via `/plugin install unforget@unforget` is verified working. Phase 3 fixes the five must-ship Tier 6 surface-survey gaps that prevent fresh init from finding real deferred work in a complex project. The goal is to take the Stuffolio fresh-init re-test from ~9 candidates (v0.1 baseline) to ≥30 candidates after Phase 3 ships.

The Tier 6 task IDs are T-2, T-5, T-6, T-10, T-11. Each is a SKILL.md spec edit. The 2026-05-02 fresh-init simulation against a scratch Stuffolio copy is the empirical basis for all five.

##### Read first

- `v0.2-release-plan.md` Phase 3 section (lines ~177-216) for the task list and ship gate.
- `v0.2-roadmap.md` Tier 6 section (lines ~66-82) for the original proposals these tasks come from. Each row in the Tier 6 table has the Stuffolio repro that justifies the fix.
- `test-reports/2026-05-02-stuffolio-fresh-init-simulation.md` if present, for the empirical basis.
- `SKILL.md` Phase 2 sections. Find them via `grep -n '## Phase 2' SKILL.md`. The five tasks edit Phase 2 Surface 1, 2, 3, plus a new Surface 1b. Read those existing sections before editing.

##### What to decide first

Two design calls before writing spec text:

1. **T-5 radar-suite source.** The release plan recommends "do both": pin radar-suite v3 schema as the supported format AND use scratch reports (`scratch/*-radar-*.md`) as the per-finding source, with `ledger.yaml` reduced to "was this audit run?" metadata. Confirm or pick one. If picking one, scratch reports are the safer choice (they don't break when radar-suite bumps schema versions).

2. **T-10 heuristic strictness.** Phase 3 plan lists four heading-text patterns ("Deferred", "Pending", "TODO", "Phase N pending"), date-prefix filenames, and "DEFERRED:" heading prefixes. Decide whether all six trigger a candidate prompt, or only a subset. Recommend: all six, but each match prompts the user (don't auto-import). Prose-shape detection is fuzzy; user confirmation is the safety net.

##### Surface area of the work

Five SKILL.md edits, one per task. Each is additive to an existing Phase 2 surface section.

1. **T-2 (filename archive exclusion).** Extend the Phase 2 universal exclusion rule. Add filename match `*archive*.md` (case insensitive) on top of the existing path-segment exclusion. One bullet added to the existing exclusion list.

2. **T-5 (radar-suite format compatibility).** Rewrite Phase 2 Surface 2 description. Specify: scratch reports `scratch/*-radar-*-YYYY-MM-DD.md` are the per-finding source. `ledger.yaml` is read for audit-run metadata only. Pin radar-suite v3+ as the supported format. List the v3 schema fields that map to UNFORGET row columns.

3. **T-6 (add `.agents/plans/` to default scan paths).** Extend Phase 2 Surface 3 default paths from `./plans/` + `./.claude/plans/` to also include `./.agents/plans/`. Generalize to `**/plans/*.md` excluding archive paths.

4. **T-10 (general doc scanning, new Surface 1b).** New section in SKILL.md immediately after Phase 2 Surface 1. Title: `### Surface 1b: General documentation scanning`. Body specifies the scan paths (`Documentation/Development/*.md`, `Documentation/Notes/*.md`, `scratch/*.md`, equivalents), the six heuristics, and that each match prompts the user (no auto-import).

5. **T-11 (recognize roadmap-shaped files).** Extend the Surface 1 regex to also match `*roadmap*.md`, `ROADMAP.md`, `*plan*.md` (case insensitive). Add a content-shape heuristic: files with 3+ headings matching `Tier N` / `Phase N` / `Priority N` patterns flag as deferral artifacts.

##### Constraints to honor

- **No spec drift.** Phase 3 changes spec content, but the changes must be additive. T-2, T-6, T-11 extend existing sections. T-5 rewrites a section, but only the content of Surface 2 (not surrounding sections). T-10 adds a new section between existing ones.
- **No SKILL.md frontmatter version bump.** Stays at `0.1.0` until Phase 5.
- **Manifest version bump to `0.2.0-phase3`** in both `plugin.json` and `marketplace.json`.
- **No emojis in committed prose** unless showing UNFORGET.md output (where 🚢 / 🌫️ are part of the format).
- **No em dashes, no double hyphens** outside of CLI flags inside backticks. Restructure sentences instead.
- **No co-authored-by lines on commits.** Push via GitHub Desktop, not Terminal.
- **`bash .claude-plugin/verify-manifest.sh` passes before each commit.**
- **Two-question interview opt-in.** Phase 3 has two open design calls (T-5, T-10) plus five spec edits. Ask the user at start of session whether to enable the interview.

##### Verification

After Phase 3 ships:

1. `bash .claude-plugin/verify-manifest.sh` exits 0.
2. Both `plugin.json` and `marketplace.json` at version `0.2.0-phase3`.
3. `grep -c '✅ shipped' v0.2-release-plan.md` returns at least 3 (Phase 1 + Phase 2 + Phase 3 stamps).
4. The two design decisions (T-5 source, T-10 heuristic strictness) appear under a `### Resolved questions` subsection in Phase 3 of `v0.2-release-plan.md`.
5. Stuffolio fresh-init re-test against the scratch copy at `~/Desktop/unforget-test-2026-05-02/` (or wherever it lives) produces ≥30 candidates. This is the Phase 3 ship gate's primary metric.
6. unforget self-test: a hypothetical fresh init run on the unforget repo itself surfaces `v0.2-roadmap.md` and `v0.2-release-plan.md` as deferral artifacts. T-11 specifically targets this regression.

Verification step 5 (the Stuffolio re-test) is the only step that requires touching the scratch copy; all other steps are repo-local. If the scratch copy has gone stale since 2026-05-02, refresh it from Stuffolio's current state before running the re-test.

##### Out of scope for this session

- Phase 4 (Tier 6 polish: T-3, T-4, T-7, T-8, T-9).
- Phase 5 (final ship: frontmatter version bump to `0.2.0`, push, tag, announce).
- v0.3-deferred items (C1, C2, C3, F1, F2, F3, F4) and the new v0.3 proposals (T1 Wake-up field, T2 Recipes section).
- Any change to the Phase 2 R1/R2/R3 spec. Phase 2 is locked.

##### What to do first when this prompt is loaded

1. Read the four "Read first" files above.
2. Apply the two-question interview opt-in (CLAUDE.md Decision Workflow). Default On for this session, since Phase 3 has two open design calls and five spec edits.
3. Ask the user the T-5 (radar-suite source) and T-10 (heuristic strictness) questions. Don't guess.
4. After answers, write a short plan file at `~/.claude/plans/<auto-named>.md` with the spec sections to edit, the design decisions captured, and the verification steps. Get plan approval via `ExitPlanMode`.
5. Execute as five plus one commits: one commit per task (T-2, T-5, T-6, T-10, T-11), each with `bash .claude-plugin/verify-manifest.sh` passing before commit. The sixth commit bumps the manifest version, stamps `v0.2-release-plan.md` Phase 3 with `✅ shipped <date>` and the Resolved questions block (mirroring the Phase 2 stamp), and may bundle small drift fixes if any spec sections drifted during the five task commits.
6. Run the Stuffolio re-test as the final ship-gate check. Phase 3 is not done until the re-test produces ≥30 candidates.

---

## Phase 4 — High-priority Tier 6 polish

**Goal:** Fix the remaining surface-survey gaps. Less critical than Phase 3 but each addresses a real adopter pain point.

### Tasks

1. **T-3 — Detect redirect-pointer files:**
   - Heuristic: short files (< 30 lines) containing pointer phrases ("MOVED", "see also", path references to UNFORGET.md or another deferral file).
   - Skip with status note rather than parse.
2. **T-4 — Hardcode known files within audit-tool dirs:**
   - Phase 2 Surface 2 spec change: only specific files within `.radar-suite/` are deferral sources. Currently: `ledger.yaml` plus `scratch/*-radar-*.md`. Other yaml in `.radar-suite/` is skipped.
3. **T-7 — Fourth state for `gh` surface:**
   - Detect missing `.git/` or non-GitHub remote before invoking `gh`.
   - Skip with: "GitHub issues skipped (project not on GitHub)."
4. **T-8 — Memory directory detection per AI tool:**
   - From Phase 1 wiring step (which AI file was wired), determine memory path.
   - Claude Code → `~/.claude/projects/<encoded>/memory/`.
   - Cursor → `~/.cursor/memory/` or equivalent (research the actual path).
   - Aider → `.aider/` or equivalent.
   - If multiple AI tools wired: scan each.
   - Document the path mapping explicitly in SKILL.md.
5. **T-9 — Content-shape check for memory meta-files:**
   - Before importing a memory file, scan for meta-doc phrases ("single source of truth", "format:", "how to use").
   - If detected: flag for user decision rather than auto-import.

### Phase 4 ship gate

Stuffolio re-test produces ≥42 candidates (≥70% of the ~60-row target). All remaining missed candidates have a clear explanation (tacit knowledge that no scan can find).

---

## Phase 5 — Documentation + ship readiness

**Goal:** Documentation is complete. Skill is releasable.

### Tasks

1. **D1 — Recommended file path in README + init flow:**
   - README "Install" / "Quick start" recommends `Documentation/Development/Deferred/UNFORGET.md` as default.
   - Init flow Phase 1 already does this, per spec.
2. **D2 — Auto-fill defaults table in SKILL.md:**
   - Currently the init flow says "conservative defaults" without specifying. Pin the values:
     - Target: `🌫️ SOMEDAY`
     - Urgency: `🟢 MEDIUM` unless source says CRITICAL/HIGH/LOW explicitly
     - Risk: Fix: `⚪ Low`
     - Risk: No Fix: `⚪ Low` unless source mentions data loss/crash/user-visible
     - ROI: `🟢 Good`
     - Blast Radius: `⚪ 1 file` unless source mentions count
     - Fix Effort: `Small`
     - Status: `Open`
3. **D3 — Re-running init policy:**
   - If `init` runs against an existing UNFORGET.md, abort with: "UNFORGET.md already exists. Use `/unforget import` to add new items, or delete the file if you want to start over."
4. **D4 — Install verification command:**
   - `/unforget --version` prints version + install path + format-version supported.
5. **D5 — Format reference in SKILL.md:**
   - Detail-block format (closure pointer / body / spawn links) currently lives only in `examples/UNFORGET.md`. Move it into SKILL.md as a first-class section.
6. **README updates for v0.2:**
   - Install instructions reflect plugin packaging.
   - Invocation form: `/unforget` not `/skill unforget`.
   - "Beta testers wanted" call-out re-added (was removed in v0.1.x cleanup) — v0.2 is the natural moment to invite real-project beta users.
   - v0.2-roadmap.md status fields updated: shipped items move to "Implemented", deferred items move to v0.3 roadmap.
7. **Version bump:**
   - `SKILL.md` frontmatter `version: 0.2.0`
   - Plugin manifest version: 0.2.0
   - Git tag: `v0.2.0`
8. **Push, announce:**
   - Push to GitHub.
   - Tag and create release notes.
   - Announce: GitHub release page, Reddit r/ClaudeAI post, mention in any Stuffolio session work.

### Phase 5 ship gate

`/unforget --version` returns 0.2.0. Stuffolio re-test passes the ≥70% threshold from Phase 4. README accurately describes v0.2 install and invocation. All Phase 1-5 commits are pushed and tagged.

---

## Test gates between phases

After each phase, re-run the Stuffolio fresh-init simulation against the scratch copy at `~/Desktop/unforget-test-2026-05-02/`. Track the candidate count progression:

| Phase | Expected candidate count | Notes |
|---|---|---|
| Baseline (v0.1) | ~9 of 60 | Per the 2026-05-02 test report. |
| After Phase 1 (plugin only) | ~9 of 60 | No spec changes; coverage unchanged. |
| After Phase 2 (recoverability) | ~9 of 60 | No surface changes; coverage unchanged. |
| After Phase 3 (must-ship Tier 6) | ~30-35 of 60 | Major jump. T-10 + T-11 + T-2 + T-6 add the missing surfaces. |
| After Phase 4 (Tier 6 polish) | ~42-50 of 60 | Edge cases handled. ~10-15 tacit-knowledge items remain unfindable. |
| Phase 5 (ship) | Same as Phase 4 | No further surface changes. |

Skip a re-test only if the phase had zero surface-survey changes (Phase 1 and Phase 2 are exempt).

---

## Risks and mitigations

| Risk | Mitigation |
|---|---|
| Plugin packaging (Phase 1) blocks on Claude Code platform docs we haven't read yet | Open question; resolve early. If plugin format is not yet documented or stable, fall back to "v0.2 ships as a polished SKILL.md with the spec fixes; v0.3 ships plugin packaging." |
| T-5 (radar-suite format compatibility) requires understanding radar-suite v3 schema in detail | Read radar-suite repo's own docs before Phase 3. radar-suite is at `/Volumes/2 TB Drive/Coding/GitHub/radar-suite/`; spec lives in its own SKILL.md. |
| Tier 6 fixes might conflict with Tier 2-4 design decisions deferred to v0.3 | Phase ordering helps: do Tier 2 (recoverability) before Tier 6 (which is destructive in some cases). Don't lock Tier 3 design decisions until v0.3 beta. |
| Stuffolio's scratch copy goes stale between tests | Keep the scratch dir until v0.2 ships. If Stuffolio's UNFORGET.md gets restructured, refresh the scratch copy. |
| v0.2 release timeline slips past Stuffolio's build 33 ship | Stuffolio doesn't depend on v0.2. The Stuffolio side is hand-maintained; v0.2 is for new adopters. No coupling. |
| Beta testers can't be recruited in time | v0.2 can ship without beta validation. Tier 3 design decisions stay deferred to v0.3 specifically because they need beta feedback. v0.2 itself is a low-risk safety + adoption-path improvement. |

---

## What v0.2 deliberately does NOT include

(Locked into v0.3 roadmap; mention here so reviewers don't re-scope.)

| Item | Why not v0.2 |
|---|---|
| C1 `/unforget close <ID>` subcommand | Non-trivial design; needs spec discussion. v0.3. |
| C2 `/unforget delete <ID>` subcommand | Same. v0.3. |
| C3 Spawn-link automation | Builds on C1. v0.3. |
| F1 Archive policy at promotion time | Permanent design decision; needs beta feedback. v0.3. |
| F2 `Hidden` Status value | Vocabulary expansion; permanent. v0.3 with beta input. |
| F3 `External` Target value | Same. v0.3 with beta input. |
| F4 Auto-derive ship gate | Lower priority; document as convention in v0.2 README. v0.3 implementation. |

---

## How to use this plan

1. Review this file. Adjust phases, ship gates, or scope before starting Phase 1.
2. When approved: change the status at the top from "Proposal" to "Approved (date)".
3. As work progresses, mark each phase's tasks complete (use a status column, or tick boxes, or just commit-message-tracking).
4. After each phase ships, run the Phase ship gate explicitly. Don't proceed to the next phase if the gate fails.
5. After v0.2 ships: archive this file (rename to `v0.2-release-plan-archive.md` or move to `releases/`). Start `v0.3-release-plan.md` for the next cycle.

---

## Estimate

| Phase | Optimistic | Realistic | Pessimistic |
|---|---|---|---|
| Phase 1 (plugin packaging) | 4 hours | 1 day | 3 days (if platform docs are weak) |
| Phase 2 (recoverability) | 4 hours | 1 day | 2 days |
| Phase 3 (critical Tier 6) | 1 day | 2-3 days | 1 week |
| Phase 4 (Tier 6 polish) | 1 day | 2 days | 4 days |
| Phase 5 (docs + ship) | 4 hours | 1 day | 2 days |
| **Total** | **~3 days** | **~7-9 days** | **~3 weeks** |

This is part-time work, not full-time. Realistic timeline assumes ~1-2 sessions per week of 2-3 hours each.
